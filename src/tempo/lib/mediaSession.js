// Promoted to src/shared/mediaSession.js once Yoru also needed background
// audio continuity. Thin re-export so this path — and Tempo's own behaviour
// — keeps working unchanged.
export { useMediaSession } from '../../shared/mediaSession'
